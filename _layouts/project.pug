---
layout: default
---

header
  div.navbar-fixed
    nav.nav-wrapper
      a(href="#" class="brand-logo center") {{ page.title }}
      ul.left
        li
          a(href='/', onclick='history.back();')
            i.material-icons(style='font-size: 40px;') chevron_left

main
  div(class='container')
    div {{ content }}
